<div class="container">

  <table class="table table-striped table-bordered">
    <tr>
      <th>Space</th>
      <th>Privacy</th>
      <th>Seated</th>
      <th>Standing</th>
    </tr>

    <% @contacts.each do |contact| %>
      <tr>
        <td><%= space.name %></td>
        <td><%= space.privacy__c %></td>
        <td><%= space.seated__c %></td>
        <td><%= space.standing__c %></td>
      </tr>
    <% end %>
  </table>

</div>
